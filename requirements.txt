PyYAML>=6.0,<7
tzdata>=2024.1
prompt_toolkit>=3.0.43,<4

Pillow>=10,<13

# Only needed for `companion app`, the local web page. Everything else in the
# kit works without them. `httpx` is needed to run the app's own tests.
fastapi>=0.110,<1
uvicorn>=0.27,<1
httpx>=0.27,<1
websockets>=14,<16
pyte>=0.8,<1
pywinpty>=2,<4; sys_platform == 'win32'

# Extract official Ollama runtime archives without requiring a system zstd tool.
zstandard>=0.23,<1

# Test suite runner
pytest>=8,<10
