Refresh {{AGENT}}'s senses: whichever of weather, daylight, important dates, follow-ups and the shape of the conversation this profile has switched on. Runs as a script with no language model. A sensor that cannot report writes nothing and removes its old file, because yesterday's weather presented as today's is worse than no weather at all.
