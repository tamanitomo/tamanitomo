Check that {{AGENT}}'s own machinery is still working: the scheduled jobs, the continuity hook, the identity files, whether a model has confirmed the present recently, memory pressure and disk space. Runs as a script with no language model. Silent when everything is fine; one message when it is not, then quiet for six hours rather than repeating.
