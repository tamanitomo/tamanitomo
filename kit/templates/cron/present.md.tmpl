Carry {{AGENT}}'s recorded present forward and reassemble ActiveContext.md. Runs as a script with no language model: it invents nothing, it repeats the last recorded state and marks it unconfirmed, so a conversation opened while the model is unavailable starts from a truthful present instead of a stale one.
