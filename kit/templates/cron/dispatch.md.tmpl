Take at most one message off {{AGENT}}'s outbox and decide whether it goes now. Runs as a script with no language model: it checks the expiry, the earliest-send time, the sleep window, whether {{HUMAN}} is visibly awake, what kind of content is permitted and how many messages have already gone today. Expired messages are dropped, never sent late. A failed send keeps its slot and is never retried.
