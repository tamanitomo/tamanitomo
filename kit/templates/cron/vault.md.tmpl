Commit whatever changed in the vault to its local git repository. Runs as a script with no language model and says nothing when nothing changed. Local only: no remote is configured and nothing is pushed anywhere. This is what makes `tamanitomo restore` possible — a bad consolidation or a file a model rewrote badly can be recovered by file, to any point in the history.
